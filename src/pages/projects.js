import React from 'react';

import { Layout, Cover } from '../layouts';

function Projects() {
    return (
        <Layout>
            <Cover
                title="Projects"
                desc="Check out the things I have created in the past and present (but not future, yet)."
            />
        </Layout>
    );
}

export default Projects;
